import React from 'react';
import { AvatarProps } from '../pb_avatar/_avatar';
declare type SourceProps = {
    aria?: {
        [key: string]: string;
    };
    className?: string;
    data?: {
        [key: string]: string;
    };
    hideIcon: boolean;
    htmlOptions?: {
        [key: string]: string | number | boolean | (() => void);
    };
    id?: string;
    source?: string;
    type: "events" | "inbound" | "outbound" | "prospecting" | "referral" | "retail" | "user";
    user: AvatarProps;
};
declare const Source: ({ aria, className, data, hideIcon, htmlOptions, id, source, type, user, }: SourceProps) => React.ReactElement;
export default Source;
