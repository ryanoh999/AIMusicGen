import React from 'react';
import { FilterDescription } from './CurrentFilters';
import { FilterBackgroundProps } from './FilterBackground';
import { SortingChangeCallback, SortOptions, SortValue } from './SortMenu';
export declare type FilterSingleProps = {
    children?: React.ReactChild[] | React.ReactChild;
    filters?: FilterDescription;
    onSortChange?: SortingChangeCallback;
    results?: number;
    sortOptions?: SortOptions;
    sortValue?: SortValue[];
} & FilterBackgroundProps;
declare const FilterSingle: ({ onSortChange, sortOptions, sortValue, filters, results, children, dark, maxHeight, minWidth, placement, ...bgProps }: FilterSingleProps) => React.ReactElement;
export default FilterSingle;
