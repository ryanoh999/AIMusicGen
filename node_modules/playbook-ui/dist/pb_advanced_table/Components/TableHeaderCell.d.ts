import React from "react";
import { Header } from "@tanstack/react-table";
import { GenericObject } from "../../types";
import { GlobalProps } from "../../utilities/globalProps";
declare type TableHeaderCellProps = {
    enableSorting?: boolean;
    enableToggleExpansion?: "all" | "header" | "none";
    handleExpandOrCollapse?: () => void;
    header?: Header<GenericObject, unknown>;
    headerChildren?: React.ReactNode | React.ReactNode[];
    loading?: boolean;
    sortIcon?: string | string[];
} & GlobalProps;
export declare const TableHeaderCell: ({ enableSorting, enableToggleExpansion, handleExpandOrCollapse, header, headerChildren, loading, sortIcon, }: TableHeaderCellProps) => JSX.Element;
export {};
