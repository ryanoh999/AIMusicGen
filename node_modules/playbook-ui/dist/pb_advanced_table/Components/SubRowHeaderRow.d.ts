import { Row, Table } from "@tanstack/react-table";
import { GenericObject } from "../../types";
import { GlobalProps } from "../../utilities/globalProps";
interface SubRowHeaderRowProps {
    collapsibleTrail?: boolean;
    enableToggleExpansion?: "all" | "header" | "none";
    onClick: (row: Row<GenericObject>) => void;
    row: Row<GenericObject>;
    subRowHeaders?: string[];
    table: Table<GenericObject>;
}
export declare const SubRowHeaderRow: ({ collapsibleTrail, enableToggleExpansion, onClick, row, subRowHeaders, table, }: SubRowHeaderRowProps & GlobalProps) => JSX.Element;
export {};
