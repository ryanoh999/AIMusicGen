import React from 'react';
import type { InputCallback } from '../types';
import { GlobalProps } from '../utilities/globalProps';
declare type Props = {
    aria?: {
        [key: string]: string;
    };
    checked?: boolean;
    children?: React.ReactNode | React.ReactNode[];
    className?: string;
    data?: {
        [key: string]: string;
    };
    disabled?: boolean;
    htmlOptions?: {
        [key: string]: string | number | boolean | (() => void);
    };
    id?: string;
    name?: string;
    onChange?: InputCallback<HTMLInputElement>;
    size?: "sm" | "md";
    value?: string;
} & GlobalProps;
declare const Toggle: ({ aria, checked, children, className, data, disabled, id, htmlOptions, name, onChange, size, value, ...props }: Props) => JSX.Element;
export default Toggle;
