import React from 'react';
import { SelectableListItemProps } from './_item.js';
import { GenericObject } from '../types';
declare type SelectableListProps = {
    aria?: {
        [key: string]: string;
    };
    children?: React.ReactElement[];
    className?: string;
    data?: GenericObject;
    htmlOptions?: {
        [key: string]: string | number | boolean | (() => void);
    };
    id?: string;
    variant?: 'checkbox' | 'radio';
};
declare const SelectableList: {
    (props: SelectableListProps): JSX.Element;
    Item: ({ aria, checked, children, className, data, defaultChecked, htmlOptions, id, label, text, name, value, variant, onChange, ...props }: SelectableListItemProps) => JSX.Element;
};
export default SelectableList;
