import React from 'react';
import { GenericObject } from '../types';
declare type SelectableIconProps = {
    aria?: {
        [key: string]: string;
    };
    checked?: boolean;
    className?: string;
    customIcon?: {
        [key: string]: SVGElement;
    };
    disabled?: boolean;
    data?: GenericObject;
    htmlOptions?: {
        [key: string]: string | number | boolean | (() => void);
    };
    icon: string;
    inputId: string;
    inputs: string;
    multi?: boolean;
    name: string;
    text: string;
    value?: string;
};
declare const SelectableIcon: ({ aria, className, checked, customIcon, data, disabled, htmlOptions, icon, inputId, inputs, multi, name, text, value, ...props }: SelectableIconProps) => React.ReactElement;
export default SelectableIcon;
