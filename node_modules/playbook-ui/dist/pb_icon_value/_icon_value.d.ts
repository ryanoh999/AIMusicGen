import React from 'react';
import { GenericObject } from '../types';
declare type IconValueProps = {
    align?: "left" | "center" | "right";
    aria?: {
        [key: string]: string;
    };
    className?: string;
    dark?: boolean;
    data?: GenericObject;
    icon: string;
    htmlOptions?: {
        [key: string]: string | number | boolean | (() => void);
    };
    id?: string;
    text: string;
};
declare const IconValue: (props: IconValueProps) => React.ReactElement;
export default IconValue;
