import React from "react";
import { VoidCallback } from "../types";
declare type FixedConfirmationToastProps = {
    autoClose?: number;
    children?: React.ReactChild[] | React.ReactChild;
    className?: string;
    closeable?: boolean;
    data?: string;
    horizontal?: "right" | "left" | "center";
    htmlOptions?: {
        [key: string]: string | number | boolean | (VoidCallback);
    };
    id?: string;
    multiLine?: boolean;
    onClose?: VoidCallback;
    open?: boolean;
    status?: "success" | "error" | "neutral" | "tip";
    text?: string;
    vertical?: "top" | "bottom";
};
declare const FixedConfirmationToast: (props: FixedConfirmationToastProps) => React.ReactElement;
export default FixedConfirmationToast;
