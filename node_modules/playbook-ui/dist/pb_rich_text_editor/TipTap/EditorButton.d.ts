import React from "react";
declare type EditorButtonProps = {
    classname?: string;
    onclick?: () => undefined | void;
    icon?: string;
    text?: string;
    disable?: boolean;
};
declare const EditorButton: ({ classname, disable, onclick, icon, text, }: EditorButtonProps) => React.ReactElement;
export default EditorButton;
